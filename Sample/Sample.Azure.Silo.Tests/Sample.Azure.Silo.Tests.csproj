<Project Sdk="Microsoft.NET.Sdk">

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />

        <PackageReference Include="Verify.Xunit" />
    </ItemGroup>

    <ItemGroup>
        <!-- The consumer assembly under test: its grains + generated
             [assembly: EdictRoutes]. Handlers live in Sample.Domain;
             Sample.Azure.Silo is the Azure host wrapper. -->
        <ProjectReference Include="../Sample.Domain/Sample.Domain.csproj" />
        <!-- The shipped in-memory Test Framework. -->
        <ProjectReference Include="../../Edict/Edict.Testing/Edict.Testing.csproj" />
        <!-- The audit drill-down's render helper (AuditMessageView) lives in the
             web component library; this test exercises it over a GetMessageAsync-decoded chain. -->
        <ProjectReference Include="../Sample.Web.Components/Sample.Web.Components.csproj" />
        <!-- For OrleansReadyHealthCheck + OrleansReadyGate; the sample-side cross-cutting
             helpers live in ServiceDefaults so both silos and Web can wire them. -->
        <ProjectReference Include="../Sample.ServiceDefaults/Sample.ServiceDefaults.csproj" />
    </ItemGroup>

</Project>
