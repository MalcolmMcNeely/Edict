@page "/claim-check"

<PageTitle>Claim Check</PageTitle>

<h1>Claim Check</h1>

<p>
    This page will host a button that places an Order whose payload exceeds the
    configured claim-check threshold, plus a pointer to the Aspire telemetry
    dashboard where the <code>envelope.shape=ClaimCheck</code> tag becomes
    visible on the publish span.
</p>

<p><em>Available in an upcoming slice.</em></p>
